apiVersion: v1
kind: Service
metadata:
  name: magicalnginx-service
spec:
  type: NodePort
  ports:
    - port: 80
      protocol: TCP
      targetPort: http
      name: http
  selector:
    app: magicalnginx
